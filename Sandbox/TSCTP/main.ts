// Pour être utilisable le source typescript doit être transformé en source javascript
// tsc source.ts --> source.js
// tsc vérifie la syntaxe du source typescript et émet des erreurs en cas d'incohérence. Process de compilation "classique"

var toto:string='Hello';

function ajout(a:number):number{
    return 1;
}

ajout(15);

//ajout(12,1);
